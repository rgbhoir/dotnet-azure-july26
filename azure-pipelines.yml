# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:  
  name: 'Default'

steps:
# 1. Explicitly checkout code with full Git history for SonarQube metrics
- checkout: self
  fetchDepth: 0 # Fixed: Disables shallow cloning to enable code tracking metrics

# 2. Prepare SonarQube Cloud Analysis Configuration
- task: SonarCloudPrepare@3
  inputs:
    SonarCloud: 'SonarCloudConnection' # Matches your service connection name
    organization: 'rgbhoir' # Your exact SonarQube Cloud organization key
    scannerMode: 'dotnet'
    projectKey: 'dotnet-azure-july26-key'
    projectName: 'dotnet-azure-july26'

# 3. Compile and Build the .NET App locally on the pipeline agent
- task: DotNetCoreCLI@2
  displayName: 'Restore & Build Solution'
  inputs:
    command: 'build'
    projects: 'dotnet-azure-july26-solution.slnx'
    arguments: '--configuration Release'

# 4. Execute the xUnit Test project and collect analysis insights
- task: DotNetCoreCLI@2
  displayName: 'Run Tests'
  inputs:
    command: 'test'
    projects: 'tests/WebApp.Tests/WebApp.Tests.csproj'
    arguments: '--configuration Release'

# 5. Execute the SonarQube Cloud Code Analysis
- task: SonarCloudAnalyze@3
  inputs:
    jdkversion: 'JAVA_HOME_17_X64'

# 6. Publish results back to SonarQube Cloud dashboard
- task: SonarCloudPublish@3
  inputs:
    pollingTimeoutSec: '300'

# 7. Package and publish Build Artifacts for your Release engine
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'
